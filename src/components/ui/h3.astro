---
interface Props {
  class?: string;
  [x: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<h3 {...rest} class:list={["scroll-m-20 text-2xl tracking-tight", className]}>
  <slot />
</h3>
